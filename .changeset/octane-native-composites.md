---
"octane-kumo": minor
---

Port DeleteResource, CommandPalette, and Flow to native Octane components.
Preserve confirmation flows, grouped command search, rich result rendering,
workflow layout/connectors, and public compound APIs with native events and refs.

Use ARIA Autocomplete/ListBox for command search and native pointer/wheel handling
for Flow. Include hydration, interaction, packed-consumer, and React-free module
checks, theme-switching previews, and documented compatibility adaptations.
