---
"octane-kumo": minor
---

Port legacy Code/CodeBlock and the isolated code and code/server highlighting
APIs to Octane Native. Keep Shiki engines, themes and language grammars lazy
and outside ordinary component imports. Preserve plain-text fallback, aliases,
line numbers/highlights, copy labels and dual themes, with native lifecycle
cleanup and keyboard-visible copy controls.

Add real JavaScript/WASM, SSR/hydration, package-consumer and React-free bundle
checks, plus a light/dark code-display preview and documented compatibility
boundaries.
