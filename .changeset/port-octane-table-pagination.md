---
"octane-kumo": minor
---

Port Table and Pagination to Octane-native compound components.

Preserve semantic tables, selection callbacks, sticky layouts, caller-controlled
pagination, legacy and compound APIs, localized labels, and native Select page
controls. Keep empty totals and committed input drafts within integer page
bounds. Add selectable resource-table SSR/hydration, packed-consumer, and
light/dark visual coverage without adding a data-grid engine.
