---
"octane-kumo": minor
---

Port Select, Autocomplete, and Combobox to Octane-native ARIA collection adapters.

Preserve Kumo application values and form serialization independently of collection keys, connect Field labels and messages, and support Combobox trigger refs, keyboard focus, Toolbar input composition, and chip keyboard removal.
